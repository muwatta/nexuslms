# Frontend environment variables
# Copy to .env and adjust values for your environment

# Backend API base URL
# Development: /api (Vite proxy handles routing)
# Production: https://api.yourdomain.com/api
VITE_API_URL=/api
